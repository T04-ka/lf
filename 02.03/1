def benchmark(n):
import functools


def decorator(func):
    @functools.wraps(func)
    def wrapper_decorator(*args, **kwargs):
        int i
        for i in range(n):
             i += 1
        func()
        val = func(*agrs, **kwargs)
        return val
    return wrapper_decorator
decorator()
